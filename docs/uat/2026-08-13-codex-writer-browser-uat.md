# Codex writer 服务端权威控制 UAT

- 日期：2026-08-13
- 被测环境：生产 Fleet Dashboard，Mac Mini M4（mesh `100.64.0.2`）
- 被测版本：`8cffd96` / Dashboard PWA v116
- Fleet 操作入口：已登录的真实浏览器页面
- 外部 writer：在 `.2` 上启动的真实 Codex CLI
- 测试数据：仅使用名称含 `UAT writer control` 的专用会话

## 验收边界

1. 所有用户可见操作必须从浏览器完成：创建会话、发送、排队、选择动作、刷新、释放和恢复写入。
2. Shell 只用于启动/终止 `.2` 上的外部 Codex 测试夹具、模拟 agent 断线，以及在 UI 已给出结论后采集服务端证据；不能代替浏览器判断通过。
3. 不操作 `peace 1.4` 或其他业务会话，不修改仓库文件，不执行强制接管其他真实任务。
4. 每个用例以浏览器可见状态为主断言；服务端快照、writer lock、rollout 和日志只作为辅证。

## 通过标准

- 浏览器不自行推断 writer、turn、access、queue 或审批状态。
- 外部 Codex 持有 writer 时，Fleet 能浏览和服务端排队，但不能 steer、停止或审批外部 turn。
- 队列状态、按钮与刷新恢复完全一致；按钮只出现服务端允许的动作。
- writer 释放、进程异常退出或 agent 重连后，页面在规定时间内恢复，不能留下长期非法占用。
- 无重复用户消息、无错误的“Codex 可接管”提示、无控制台错误。

## UAT 用例

| ID | 场景 | 浏览器操作 | 预期结果 | 状态 |
|---|---|---|---|---|
| UAT-01 | 浏览器新建专用线程 | 选择 `.2` → Codex → 新建项目会话；在首次发送前选择审批模式，发送初始化消息 | 首次发送才创建真实 thread；审批选择不被首个控制快照覆盖；消息与回复各出现一次 | **部分通过**：thread 由首次浏览器发送创建，`请求批准` 保留；用户消息仅一次，但无助手回复或可见失败原因 |
| UAT-02 | 外部 Codex 获取 writer | 从 `.2` 外部 Codex resume UAT thread；浏览器刷新并重新进入 | 标题显示“其他 Codex 客户端正在使用 · Fleet 只读同步”；输入框仍允许编辑以便排队；空输入发送按钮禁用且不出现“释放会话” | **通过** |
| UAT-03 | 外部 turn 期间提交消息 | 在浏览器输入专用排队消息并点击提交 | 用户消息只出现一次；下方显示外部占用确认卡；仅显示“等待并发送 / 强制接管 / 取消”，不显示 steer/停止 | **通过** |
| UAT-04 | 选择等待并刷新恢复 | 点击“等待并发送”，随后刷新页面并重新进入同一会话 | 队列变为等待 writer；刷新后仍存在且文案/按钮一致；消息不丢失、不重复、不自动在浏览器本地改为 sent | **通过** |
| UAT-05 | 外部 writer 正常释放后自动投递 | 让 `.2` 外部 Codex 正常完成，保持浏览器打开 | 服务端自动取得 writer 并发送排队消息；页面出现真实回复；队列卡消失；最终 writer 自动释放 | **部分通过**：自动取得 writer、队列卡消失、最终释放均通过；Fleet 回复未显示，且无可见失败原因 |
| UAT-06 | Fleet turn 手动释放 | 从浏览器发送要求持续运行的 UAT 消息；出现 Fleet writer 后点击“释放会话” | 当前 Fleet 输出停止；页面进入只读；writer 未清空前显示“仍由 Fleet 占用/等待回收”，不得提前显示“Codex 可接管” | **通过**：本次释放原子完成，直接进入已确认的可接管态 |
| UAT-07 | 只读完成态与恢复写入 | 等 writer 清空后观察页面，再点击“恢复 Fleet 写入” | 清空后显示“Fleet 只读，Codex 可接管”；输入、附件、审批和模型设置禁用；恢复后回到可写且不凭浏览器生成 writer | **通过** |
| UAT-08 | 外部 writer 异常退出回收 | 再次从 `.2` 启动外部 Codex 持锁，浏览器确认外部占用后异常终止该测试进程 | 浏览器不长期保留外部占用；服务端下一次快照清除 writer；遗留 lock 文件即使存在也无进程持有 | **失败**：进程与 lock holder 均消失后，页面超过 60 秒仍显示外部占用；重启 fleet-agent 后仍复现 |
| UAT-09 | 控制快照过期保护 | UAT thread 空闲时停止 `.2` fleet-agent 超过 10 秒，留在浏览器观察；再恢复 agent | 页面显示“会话控制状态不可确认 · 正在重连”，全部 mutation 禁用；不把旧 writer/turn/access 改成臆测值；恢复后自动回到服务端真实状态 | **通过**：停服 13 秒后全部 mutation 禁用；恢复后约 1.5 秒采用服务端快照 |
| UAT-10 | 浏览器质量门禁 | 全流程观察控制台；关键节点检查按钮 enabled/disabled、刷新与重复消息 | PWA 资源为 v116；无未处理 JS error；状态文案和按钮与服务端动作一致 | **通过**：`app.js/style.css/chat_model.js` 均为 v116；浏览器 error/warning 为 0 |

## 退出与清理

1. 确保 `.2` 上没有本轮启动的外部 Codex 进程。
2. 确保 fleet-agent 处于运行且 `/api/health` 返回 `ok`。
3. UAT 专用会话最终设为 `read_write + idle + no writer + empty visible queue`，随后从浏览器归档。
4. 记录未通过项的浏览器表现、发生时间和服务端辅证；先报告，不用 API 绕过失败步骤。

## 执行记录

### 结论

- 时间：2026-08-13 18:15–18:33（Asia/Shanghai）。
- 真实浏览器 UAT 会话：`019ffa9e-79aa-7523-91b2-3094c37477fe`。
- 结果：7 项通过，2 项部分通过，1 项失败；**不满足整体放行标准**。
- 控制面主路径通过：外部 writer 识别、服务端排队、刷新恢复、正常释放后自动投递、Fleet 手动释放、只读/恢复写入、快照过期保护均按预期工作。

### 关键浏览器证据

1. 外部 Codex 持有 writer 时，页面显示“其他 Codex 客户端正在使用 · Fleet 只读同步”和“Codex Desktop 正在输出”；输入可编辑，但没有“释放会话”和“停止生成”。
2. 浏览器提交排队消息后仅渲染一个用户消息；确认卡先仅有“等待并发送 / 强制接管 / 取消”。选择等待后变为“已由服务器排队”，仅保留“强制接管 / 取消”。
3. 刷新会清空当前页面选择，但重新打开同一会话后，服务端队列、确认按钮与单条消息完整恢复。
4. 外部 Codex 正常完成后，无需刷新即从外部占用切换为 Fleet 占用，排队卡消失；浏览器点击“释放会话”后显示“Fleet 只读，Codex 可接管”，输入、附件、审批、模型和发送均禁用；“恢复 Fleet 写入”后恢复可用。
5. fleet-agent 停止 13 秒后，页面显示“会话控制状态不可确认 · 正在重连”，不再显示旧外部占用，全部 mutation 控件禁用；agent 恢复后约 1.5 秒退出重连态。
6. 全流程浏览器控制台没有 error 或 warning；加载资源为 Dashboard PWA v116。

### 未通过项

#### UAT-D01（阻断）：外部 writer 进程异常退出后留下服务端幽灵占用

- `.2` 上真实 Codex CLI 已先被浏览器确认持有 writer；随后精确终止持锁 PID。
- `lsof` 确认 lock 文件无 holder，`pgrep` 确认 Codex CLI 与 `sleep 90` 均不存在，fleet-agent 健康。
- 浏览器连续 60 秒仍显示“其他 Codex 客户端正在使用 · Fleet 只读同步 / Codex Desktop 正在输出”；重启 fleet-agent 后仍恢复为同一错误状态。
- 只有再次用外部 Codex resume 并让一个新 turn 正常完成后，页面才在约 1.2 秒内清除外部占用。
- 代码辅证：当前 `Control` 在 rollout 仍为 active 且找不到物理 lock owner 时仍回退为 `desktop`；定时 orphan reaper 只枚举 Fleet sidecar writer，没有覆盖外部 writer。这会让服务端队列永久等待一个已不存在的进程。

#### UAT-D02（阻断）：Fleet turn 失败时浏览器无回复也无错误反馈

- 浏览器首条初始化消息和后续独立 Fleet 消息都能进入 Fleet writer 状态，用户消息各只出现一次。
- 独立 Fleet 消息约 60–90 秒后自动清除 writer，但没有助手消息、错误卡或 toast。
- 同期外部 Codex CLI 多次显示 WebSocket/HTTPS timeout 后能够 fallback 并最终正常回复，说明本轮网络/模型服务确有明显抖动；但 Fleet 页面把失败静默处理仍不符合真实用户预期。
- 因此 UAT-01 与 UAT-05 只判部分通过，不能把它们当成单纯“模型慢”而判绿。

### 清理结果

- 两个 UAT 专用会话均已从浏览器归档。
- `.2` 的 fleet-agent 已恢复运行，`/api/health` 返回 `ok`。
- 两个 UAT thread 的 writer lock 文件都无进程持有；无 `codex exec resume` 测试进程残留。
